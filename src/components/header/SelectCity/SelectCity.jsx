import styles from './selectCity.module.css'

export const SelectCity = () => {
    return (
        <select className={styles.select_city}>
            <option value="someOption">Երևան</option>
        </select>
    )
}

